#include<stdio.h> 
int main() { 
int rows=5,i,j,k,m=1; 
for(i=1;i<=rows;i--) {
    for(j=1;j<=m;j++) { 
        printf(" "); } 
    for(k=1;k<=(2*i-1);k++) { 
        printf("*"); } 
        m++; 
    printf("\n"); } 
    return 0; 
}
